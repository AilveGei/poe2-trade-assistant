{
  "name": "poe2-trade-assistant",
  "version": "0.1.0",
  "description": "流放之路2 腾讯服 市集交易助手",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:content -- --watch & npm run build:background",
    "build": "npm run build:content && npm run build:background && npm run copy:manifest",
    "build:content": "vite build --mode content",
    "build:background": "vite build --mode background",
    "copy:manifest": "cp manifest.json dist/manifest.json",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "@types/chrome": "^0.0.260"
  }
}
